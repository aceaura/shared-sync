name: sync_engine
description: Shared directory sync engine backed by a hidden git snapshot repo.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  path: ^1.9.0
  crypto: ^3.0.0
  sqlite3: ^2.4.0
  watcher: ^1.1.0
  args: ^2.4.0

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
